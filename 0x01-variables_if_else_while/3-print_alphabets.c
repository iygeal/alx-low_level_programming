#include <stdio.h>
/** main - This program prints alphabets A-Z and a-z.
 * Return: 0 = Success
 */
int main(void)
{
	char x = 'a'
	char y = 'A'

	while
	{
		(x <= z)
		putchar(x);
	}
	while
	{
		(y <= Z)
		putchar(y);
	}
	putchar('\n');
	return (0)
}
