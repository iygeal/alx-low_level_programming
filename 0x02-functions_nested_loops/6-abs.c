#include "main.h"
/**
 * _abs - Prints absolute value of integer.
 *@x: The integer to be checked and printed.
 * Return: 0 = success
 */
int _abs(int x)
{
	if (x > 0)
		return (-x);
	else if (x >= 0)
		return (x);

return (0);
}
