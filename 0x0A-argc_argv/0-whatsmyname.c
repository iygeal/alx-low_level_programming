#include <stdio.h>
#include <stdlib.h>
/**
 * main - The main function which prints name of the program
 * @argc: Argument count
 * @argv: The argument vector
 * Return: 0 = success
 */

int main(int __attribute__((unused))argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
